"""provenas — distilling Python logic into small neural networks.

Thin shared library. Pure-Python/numpy modules (calculator, strata, generate,
dataset, metrics) import without torch; torch is only needed for models, losses,
train, and the experiment scripts.
"""
